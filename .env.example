# Optional: analytics (app works fine without this)
# NEXT_PUBLIC_POSTHOG_KEY=
# NEXT_PUBLIC_POSTHOG_HOST=https://eu.i.posthog.com

# Optional: needed only for data:build / data:refresh scripts (SIRENE API)
# SIRENE_API_TOKEN=
